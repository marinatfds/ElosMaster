# App antigo (arquivado)

`client/` (Create React App + React 17) e `server/` (Koa + SQL Server) são a versão anterior
do ElosMaster, mantida aqui só como referência histórica até o cutover para a nova stack
(`apps/web` + `apps/api`, na raiz do repositório) ser confirmado como estável em produção.

Não recebe mais atualizações. Veja `DEPLOY.md` na raiz para o checklist de cutover e o motivo
de cada parte ter sido reescrita.
